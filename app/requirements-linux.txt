# KALKI Linux core runtime
SpeechRecognition>=3.10.0
edge-tts>=6.1.9
pygame>=2.5.0
psutil>=5.9.0
Pillow>=10.0.0
requests>=2.31.0
pytesseract>=0.3.10
customtkinter>=5.2.0
cryptography>=42.0.0
groq>=0.4.2
darkdetect>=0.8.0

# Integrations
spotipy>=2.23.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
pywhatkit>=5.4
yt-dlp>=2024.0.0
pyTelegramBotAPI>=4.15.0

# Optional offline wake word and deep scan
vosk>=0.3.45
playwright>=1.40.0

# SaaS integrations
google-api-python-client>=2.100.0
sentry-sdk>=1.39.0
posthog>=3.0.2
firebase-admin>=6.3.0

# Document ingestion and secret storage
pypdf>=3.17.0
docx2txt>=0.8
openpyxl>=3.1.2
python-pptx>=0.6.21
keyring>=24.3.0
curl_cffi>=0.7.1

# Optional Linux microphone support. PyAudio may require the system package
# portaudio19-dev before installation; the dashboard remains usable without it.
# pyaudio>=0.2.14

# Deliberately omitted: pywin32, pycaw, comtypes, wmi, pywebview, and pystray.
# The Linux launcher uses the default browser and XDG paths instead.
